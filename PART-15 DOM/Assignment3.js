// 1. Select the anchor tag
const selectAnchor = document.querySelector("a")
// 2. Use getAttribute(attrName) to check the attribute
console.log(selectAnchor.getAttribute("href"))
// 3. select a two & set "href" attribute setAttrin=bute(attrName,value)
const search = document.querySelector(".a-two")
search.setAttribute("href","https://www.youtube.com")
console.log(search)